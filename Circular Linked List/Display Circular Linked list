#include <iostream>

using namespace std;

struct Node
{
    int data;
    struct Node *next;
};

struct Node *head=NULL;

void Create(int A[],int n)
{
    int i;
    struct Node *temp, *last;
    head = new Node;
    head->data = A[0];
    head->next = head;
    last = head;
    
    for(i=1; i<n; i++)
    {
        temp=new Node;
        temp->data=A[i];
        temp->next=last->next;
        last->next = temp;
        last=temp;
    }
    
}

// Iterative Display of Circular Linked List // 
void Display(struct Node* p)
{
    do
    {
        cout<<p->data<<"->";
        p=p->next;
        
    } while(p!=head);
    cout<<endl;
}

// Recursive Display of Circular Linked List // 
void RDisplay(struct Node *p)
{
    static int flag=0;
    if(p!=head || flag==0)
    {
        flag=1;
        cout<<p->data<<"->";
        RDisplay(p->next);
    }
    flag=0;
}

int main()
{
    int A[]={10,20,30,40,50};
    Create(A,5);
    Display(head);
    RDisplay(head);

    return 0;
}
